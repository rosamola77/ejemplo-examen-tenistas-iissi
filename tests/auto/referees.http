
### This is an auto-generated test suite, it needs to be completed with valid data.
### These are not all tests you need, more of them should be created to evaluate the functional
### requirements of your project. These tests only test the CRUD endpoints of the entity.
### Silence is a DEAL research team project, more info about us in https://deal.us.es
@BASE = http://127.0.0.1:8080/api/v1

### Auxiliary query
### Positive test
### Test 00: Get all existing referees
### This query is used in several of the below tests it should be executed first.
# @name referees
GET {{BASE}}/referees

### Login a(n) users and save the generated token 
### This token is used in several of the below tests it should be executed second.
# @name login
POST {{BASE}}/login 
Content-Type: application/json 

{ 
	"username": ###REPLACE###,
	"password": ###REPLACE###
}

###
@token = {{login.response.body.sessionToken}}


### TESTS BEGIN

### Test 01: Get one existing referees by its id.
### positive test 
@refId = {{referees.response.body.0.referee_id}}
GET {{BASE}}/referees/{{refId}} 
Content-Type: application/json 


### Test 02: Try get one existing referees by its nonexistent id.
### negative test 
GET {{BASE}}/referees/999999999
Content-Type: application/json 


### Test 03: Add a new referees successfully
### Positive test 
### We assume that the token has been aquired by the login request.
# @name newreferees
POST {{BASE}}/referees 
Content-Type: application/json 
Token: {{token}} 

{
	"license": ###REPLACE###
} 

### Check the created referees

@newrefereesid = {{newreferees.response.body.lastId}}
GET {{BASE}}/referees/{{newrefereesid}}
Content-Type: application/json 

### Test 04: Add a new referees without a session token
### Negative test 
POST {{BASE}}/referees 
Content-Type: application/json 

{ 
	"license": ###REPLACE###
}

### Test 05: Modify an existing referees 
### Positive test 
@refId = {{referees.response.body.0.referee_id}} 
PUT {{BASE}}/referees/{{refId}} 
Content-Type: application/json 
Token: {{token}} 

{ 
	"license": ###REPLACE###
}

### Check the modified referees

GET {{BASE}}/referees/{{newrefereesid}}
Content-Type: application/json 

### Test 06: Try to modify an existing referees without a session token
### Negative test
@refId = {{referees.response.body.0.referee_id}}
PUT {{BASE}}/referees/{{refId}}
Content-Type: application/json

{
	"license": ###REPLACE###
}

### Test 07: Delete an existing referees
### Positive test

### Create a new referees, which we will delete
# @name createdrefToDelete
POST {{BASE}}/referees
Content-Type: application/json
Token: {{token}}

{
	"license": ###REPLACE###
}

### Check the created referees
@refdelId = {{createdrefToDelete.response.body.lastId}}
GET {{BASE}}/referees/{{refdelId}}
Content-Type: application/json 

### Delete the referees
DELETE {{BASE}}/referees/{{refdelId}}
Token: {{token}}

### Check the deleted referees
GET {{BASE}}/referees/{{refdelId}}
Content-Type: application/json 

### Test 08: Try to delete a referees without a session token
### Negative test
DELETE {{BASE}}/referees/{{refId}}

