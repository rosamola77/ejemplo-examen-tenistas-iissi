
### This is an auto-generated test suite, it needs to be completed with valid data.
### These are not all tests you need, more of them should be created to evaluate the functional
### requirements of your project. These tests only test the CRUD endpoints of the entity.
### Silence is a DEAL research team project, more info about us in https://deal.us.es
@BASE = http://127.0.0.1:8080/api/v1

### Auxiliary query
### Positive test
### Test 00: Get all existing players
### This query is used in several of the below tests it should be executed first.
# @name players
GET {{BASE}}/players

### Login a(n) users and save the generated token 
### This token is used in several of the below tests it should be executed second.
# @name login
POST {{BASE}}/login 
Content-Type: application/json 

{ 
	"username": ###REPLACE###,
	"password": ###REPLACE###
}

###
@token = {{login.response.body.sessionToken}}


### TESTS BEGIN

### Test 01: Get one existing players by its id.
### positive test 
@plaId = {{players.response.body.0.player_id}}
GET {{BASE}}/players/{{plaId}} 
Content-Type: application/json 


### Test 02: Try get one existing players by its nonexistent id.
### negative test 
GET {{BASE}}/players/999999999
Content-Type: application/json 


### Test 03: Add a new players successfully
### Positive test 
### We assume that the token has been aquired by the login request.
# @name newplayers
POST {{BASE}}/players 
Content-Type: application/json 
Token: {{token}} 

{
	"ranking": ###REPLACE###,
	"active": ###REPLACE###,
	"trainer_id": ###REPLACE###
} 

### Check the created players

@newplayersid = {{newplayers.response.body.lastId}}
GET {{BASE}}/players/{{newplayersid}}
Content-Type: application/json 

### Test 04: Add a new players without a session token
### Negative test 
POST {{BASE}}/players 
Content-Type: application/json 

{ 
	"ranking": ###REPLACE###,
	"active": ###REPLACE###,
	"trainer_id": ###REPLACE###
}

### Test 05: Modify an existing players 
### Positive test 
@plaId = {{players.response.body.0.player_id}} 
PUT {{BASE}}/players/{{plaId}} 
Content-Type: application/json 
Token: {{token}} 

{ 
	"ranking": ###REPLACE###,
	"active": ###REPLACE###,
	"trainer_id": ###REPLACE###
}

### Check the modified players

GET {{BASE}}/players/{{newplayersid}}
Content-Type: application/json 

### Test 06: Try to modify an existing players without a session token
### Negative test
@plaId = {{players.response.body.0.player_id}}
PUT {{BASE}}/players/{{plaId}}
Content-Type: application/json

{
	"ranking": ###REPLACE###,
	"active": ###REPLACE###,
	"trainer_id": ###REPLACE###
}

### Test 07: Delete an existing players
### Positive test

### Create a new players, which we will delete
# @name createdplaToDelete
POST {{BASE}}/players
Content-Type: application/json
Token: {{token}}

{
	"ranking": ###REPLACE###,
	"active": ###REPLACE###,
	"trainer_id": ###REPLACE###
}

### Check the created players
@pladelId = {{createdplaToDelete.response.body.lastId}}
GET {{BASE}}/players/{{pladelId}}
Content-Type: application/json 

### Delete the players
DELETE {{BASE}}/players/{{pladelId}}
Token: {{token}}

### Check the deleted players
GET {{BASE}}/players/{{pladelId}}
Content-Type: application/json 

### Test 08: Try to delete a players without a session token
### Negative test
DELETE {{BASE}}/players/{{plaId}}

