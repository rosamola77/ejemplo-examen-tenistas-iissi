
### This is an auto-generated test suite, it needs to be completed with valid data.
### These are not all tests you need, more of them should be created to evaluate the functional
### requirements of your project. These tests only test the CRUD endpoints of the entity.
### Silence is a DEAL research team project, more info about us in https://deal.us.es
@BASE = http://127.0.0.1:8080/api/v1

### Auxiliary query
### Positive test
### Test 00: Get all existing sets
### This query is used in several of the below tests it should be executed first.
# @name sets
GET {{BASE}}/sets

### Login a(n) users and save the generated token 
### This token is used in several of the below tests it should be executed second.
# @name login
POST {{BASE}}/login 
Content-Type: application/json 

{ 
	"username": ###REPLACE###,
	"password": ###REPLACE###
}

###
@token = {{login.response.body.sessionToken}}


### TESTS BEGIN

### Test 01: Get one existing sets by its id.
### positive test 
@setId = {{sets.response.body.0.set_id}}
GET {{BASE}}/sets/{{setId}} 
Content-Type: application/json 


### Test 02: Try get one existing sets by its nonexistent id.
### negative test 
GET {{BASE}}/sets/999999999
Content-Type: application/json 


### Test 03: Add a new sets successfully
### Positive test 
### We assume that the token has been aquired by the login request.
# @name newsets
POST {{BASE}}/sets 
Content-Type: application/json 
Token: {{token}} 

{
	"match_id": ###REPLACE###,
	"winner_id": ###REPLACE###,
	"set_order": ###REPLACE###,
	"score": ###REPLACE###
} 

### Check the created sets

@newsetsid = {{newsets.response.body.lastId}}
GET {{BASE}}/sets/{{newsetsid}}
Content-Type: application/json 

### Test 04: Add a new sets without a session token
### Negative test 
POST {{BASE}}/sets 
Content-Type: application/json 

{ 
	"match_id": ###REPLACE###,
	"winner_id": ###REPLACE###,
	"set_order": ###REPLACE###,
	"score": ###REPLACE###
}

### Test 05: Modify an existing sets 
### Positive test 
@setId = {{sets.response.body.0.set_id}} 
PUT {{BASE}}/sets/{{setId}} 
Content-Type: application/json 
Token: {{token}} 

{ 
	"match_id": ###REPLACE###,
	"winner_id": ###REPLACE###,
	"set_order": ###REPLACE###,
	"score": ###REPLACE###
}

### Check the modified sets

GET {{BASE}}/sets/{{newsetsid}}
Content-Type: application/json 

### Test 06: Try to modify an existing sets without a session token
### Negative test
@setId = {{sets.response.body.0.set_id}}
PUT {{BASE}}/sets/{{setId}}
Content-Type: application/json

{
	"match_id": ###REPLACE###,
	"winner_id": ###REPLACE###,
	"set_order": ###REPLACE###,
	"score": ###REPLACE###
}

### Test 07: Delete an existing sets
### Positive test

### Create a new sets, which we will delete
# @name createdsetToDelete
POST {{BASE}}/sets
Content-Type: application/json
Token: {{token}}

{
	"match_id": ###REPLACE###,
	"winner_id": ###REPLACE###,
	"set_order": ###REPLACE###,
	"score": ###REPLACE###
}

### Check the created sets
@setdelId = {{createdsetToDelete.response.body.lastId}}
GET {{BASE}}/sets/{{setdelId}}
Content-Type: application/json 

### Delete the sets
DELETE {{BASE}}/sets/{{setdelId}}
Token: {{token}}

### Check the deleted sets
GET {{BASE}}/sets/{{setdelId}}
Content-Type: application/json 

### Test 08: Try to delete a sets without a session token
### Negative test
DELETE {{BASE}}/sets/{{setId}}

